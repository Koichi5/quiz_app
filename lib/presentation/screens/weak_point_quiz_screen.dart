import 'package:flutter/material.dart';

class WeakPointQuizScreen extends StatelessWidget {
  const WeakPointQuizScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
